## 修复拖拽和点击冲突问题

### 1. 修复 content.tsx 中的拖拽逻辑
- 在 `makeDraggable` 方法中添加拖拽距离追踪
- 添加最小拖拽阈值（如 5px），小于此值视为点击
- 在点击事件中检查是否发生了拖拽，如果是则阻止点击
- 优化弹窗头部拖拽，确保关闭按钮不受影响

### 2. 修复 App.tsx 中的 UI 问题
- 移除无效的 `WebkitAppRegion: 'drag'` 属性
- 保持现有的 `WebkitAppRegion: 'no-drag'` 用于可交互区域

### 3. 具体修改内容
- **content.tsx**:
  - 添加 `dragStartPosition` 追踪初始鼠标位置
  - 添加 `hasDragged` 标志判断是否发生拖拽
  - 在浮动球点击事件中检查 `hasDragged`
  - 优化拖拽事件处理逻辑

- **App.tsx**:
  - 移除 Header 组件中的 `WebkitAppRegion: 'drag'`